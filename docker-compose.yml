version: '3.8'
services:
  gtcha-bot:
    build: .
    container_name: gtcha-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
